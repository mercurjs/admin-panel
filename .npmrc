# Sub-repo isolation from super-root pnpm workspace
# admin-panel is a standalone submodule, NOT part of GP root workspace
# Per v160-2-1 OQ-1 resolution: pnpm + standalone sub-repo
ignore-workspace=true
